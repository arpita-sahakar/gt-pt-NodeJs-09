// const sum = (el1, el2)=>{
//     let sumResult = el1 + el2;
//     return sumResult;
// };
//  const difference = (el1, el2)=> el1-el2;
//  const product = (el1, el2) => el1*el2;
//  const quotient = (el1, el2) => el1/el2;

//  module.exports = {
//      sum,
//      difference,
//      product,
//      quotient,
//  }
//  ---------------------------------------------------------------------

const sum = (el1, el2) => el1 + el2;
const difference = (el1, el2) => el1 - el2;
const product = (el1, el2) => el1 * el2;
const quotient = (el1, el2) => el1 / el2;

module.export = {
  sum,
  difference,
  product,
  quotient,
};
